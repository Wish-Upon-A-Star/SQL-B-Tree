[project]
name = "locust-tcp-test"
version = "0.1.0"
description = "Locust-based TCP throughput sweep for SQL-B-Tree"
requires-python = ">=3.9"
dependencies = [
    "locust>=2.0",
    "matplotlib>=3.5",
]

[tool.uv]
package = false
