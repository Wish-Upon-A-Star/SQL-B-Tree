digraph CmdProcessorOverallArchitecture {
    graph [
        rankdir=LR,
        compound=true,
        nodesep=0.35,
        ranksep=0.7,
        splines=line,
        newrank=true,
        pad=0.2,
        bgcolor="transparent"
    ];

    node [
        shape=box,
        style="rounded,filled",
        fillcolor="white",
        color="#64748b",
        fontname="Pretendard",
        fontsize=11,
        margin="0.14,0.09"
    ];

    edge [
        color="#475569",
        fontname="Pretendard",
        fontsize=10,
        arrowsize=0.7
    ];

    subgraph cluster_io {
        label="입출력 구현체";
        style="rounded,filled";
        color="#cbd5e1";
        fillcolor="#f8fafc";
        fontname="Pretendard";
        fontsize=13;
        margin=14;

        io_impl [
            label="TCP / REPL / tests\n입력 수신\n출력 형식 관리",
            fillcolor="#ffffff"
        ];
    }

    subgraph cluster_core {
        label="코어";
        style="rounded,filled";
        color="#bae6fd";
        fillcolor="#f0f9ff";
        fontname="Pretendard";
        fontsize=13;
        margin=14;

        cmd_processor [
            label="CmdProcessor\n요청 처리 정책\nrequest/response 소유",
            fillcolor="#ffffff"
        ];

        db_black_box [
            label="DB 실행\nBlackBox",
            fillcolor="#ffffff",
            color="#cbd5e1"
        ];
    }

    cmd_contract [
        label="CmdRequest / CmdResponse",
        fillcolor="#dbeafe",
        color="#60a5fa",
        margin="0.08,0.04"
    ];

    io_impl -> cmd_contract [
        ltail=cluster_io,
        arrowhead=none,
        minlen=2,
        weight=10
    ];
    cmd_contract -> cmd_processor [
        lhead=cluster_core,
        minlen=2,
        weight=10
    ];
    cmd_processor -> db_black_box [label="DB 의존", fontsize=9, minlen=1];
}
